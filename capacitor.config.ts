import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.gpiroux.ionicTranslate',
  appName: 'iTranslate',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
